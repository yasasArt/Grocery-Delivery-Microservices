[project]
name = "delivery-service"
version = "0.1.0"
description = "Delivery service for grocery delivery microservices"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "sqlalchemy>=2.0.0",
]